{
  "name": "gomila-site",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed for static sites'",
    "start": "http-server public -p $PORT"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
