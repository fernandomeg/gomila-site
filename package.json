{
  "name": "gomila-site",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/",
    "start": "http-server dist -p $PORT"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": "20.x"
  }
}
